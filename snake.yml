name: generate animation

on:
  schedule:
    - cron: "0 0 * * *"  # Ejecutar diariamente a medianoche
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ismael-vergara
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Push animation to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update README.md with the latest animation
        run: |
          sed -i 's|<img src="https://raw.githubusercontent.com/ismael-vergara/ismael-vergara/output/.*" alt="Snake animation" />|<img src="https://raw.githubusercontent.com/ismael-vergara/ismael-vergara/output/github-contribution-grid-snake.svg" alt="Snake animation" />|' README.md
          
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add README.md
          git commit -m "Update Snake animation in README"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
